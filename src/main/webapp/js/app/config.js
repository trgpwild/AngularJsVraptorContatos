var AppName = 'Contatos';

var limit = 10;

var baseUrl = '/' + AppName;
var homePage = '/' + AppName + '/';
var loginPage = '/' + AppName + '/login.html';

function string(val) {
	return "" + val + "";
}